/**
 * Public API of the `settings` feature — account settings (appearance,
 * notifications, security, danger zone). UI-first with mock actions.
 */
export { SettingsPanel } from './components/settings-panel'
